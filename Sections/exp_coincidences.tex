\subsection{Correlation Value}
\label{subsec:CorrVal}
First measurements measure second order coherence at $\tau = 0$ i.e $g^{(2)}(0)$. The procedure is given bellow. 
\begin{enumerate}
    \item Feed the Pulsed Laser into the optical setup in figure \ref{fig:exp_lay}. 
    \item Select a Laser frequency: $1/T_{dead} \approx 2\cdot10^8 = 200 MHz$  . 
    \item Select Exposure Time. 
    \item Connect Laser Sync and SPAD channels into the time tagger. 
    \item Measure timestamps of detection events. 
    \item Compare Channel Ticks. 
    \begin{itemize}
        \item[]  $\Delta t_{coincidence} = \Delta t_{B} - \Delta t_{A} $
        \item[] Coincidence if $\Delta t_{coincidence} \leq 2 \cdot Jitter$
    \end{itemize}
    \item Calculate Probabilities 
    \begin{itemize}
        \item[] $P_{A|B} = \frac{\# Events_{A|B}}{\# Pulses}$
        \item[] $P_{AB} = \frac{\# Coincidences}{\# Pulses}$
    \end{itemize}
    \item Calculate Correlation 
    \begin{itemize}
        \item[] $g^{(2)}(0) = \frac{P_{AB}}{P_{A} \cdot P_{B}} $
    \end{itemize}
    \item Check 3, 4 fold correlations $g^{(3)}, g^{(4)}, ...$
\end{enumerate}


\subsubsection{Pulsed Laser Measurements (05.06.2026)}
Measurement of $g^2(\tau=0)$ where taken with 10 ps resolution with a laser attenuation adjusted to obtain $~2\%$ of total pulses detected by SPADs. 
Results are summarized in Figure \ref{fig:g2(0)_measurements_80MHz_1MHz_run1}.\\
\begin{figure}[H]
    \centering
    \includegraphics[width = 0.5\textwidth]{Figures/g2(0)_measurements_80MHz_1MHz_run1.png}
    \caption{$g^2(0)$ measurement detailed in experiment \ref{subsec:CorrVal}.}
    \label{fig:g2(0)_measurements_80MHz_1MHz_run1}
\end{figure}

These results where obtained in the laboratory session following the detailed steps bellow: 

\begin{figure}[h]
    \centering
    \includegraphics[width = 0.5\textwidth]{Figures/optical_path_calibration_1MHz_run1.png}
    \caption{Differences in signal times. Calibration using \texttt{tt.Correlation} function}
    \label{fig:optical_path_calibration_1MHz_run1}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width = 0.5\textwidth]{Figures/optical_path_calibrated_1MHz_run1.png}
    \caption{Calibrated signals to zero ps delay.}
    \label{fig:optical_path_calibrated_1MHz_run1}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width = 0.5\textwidth]{Figures/jitter_run1.png}
    \caption{Optical Setup Jitter made up of Swabian, LASER $\&$ SPAD.}
    \label{fig:jitter_run1}
\end{figure}


\begin{itemize}
    \item Connect SPAD A, SPAD B, and Laser Sync to Swabian channel 1,2 and 3 respectively. 
    \item Measure Power at $100 \mu W$
    \item Read the Pico-Quant LASER at $36.8\%$ power. 
    \item LASER frequency is configured at $1MHz$. Select internal oscillator\textit{Source: Int2}, and Rep Frequency at \textit{1}.
    \item Align the LASER to the SPADs active areas. 
    \item Place ND0.6 Filter.
    \item Isolate the system from external light sources.
    \item Calibrate. Set baseline for SPAD dark counts and Laser clock. Laser head remains off.  
    \begin{itemize}
        \item Use \texttt{tt.Countrate} to extract average counts-per-second (cps) over $5*10^{11}$ picoseconds. 
        \item Averaging the previus averages 65 times, we get:
        \begin{itemize}
            \item Mean SPAD A: 9.908 cps
            \item Mean SPAD B: 9,816.862  cps
            \item Mean LASER Sync: 999,995.969 cps
        \end{itemize} 
    \end{itemize}
    \item Turn Laser On and calibrate cps. 
    \begin{itemize}
        \item Use \texttt{tt.Countrate} to extract average counts-per-second (cps) over $5*10^11$ picoseconds. 
        \item Averaging the previus averages 94 times, we get:
        \begin{itemize}
            \item Mean SPAD A: 26,262.771 cps ($2.6\%$ of pulses detected)
            \item Mean SPAD B: 30,842.627 cps ($3\%$ of pulses detected)
            \item Mean LASER: 999,995.819 cps
        \end{itemize}
    \end{itemize}
    \item Then, we proceed with the calibration for the optical path differences. We use \texttt{tt.Correlation} function, with $10$ps resolution to find the time differences between two channels and obtained the following results. 
    \begin{itemize}
        \item Sync $\rightarrow$ SPAD A Delays:
        \item[] $ [-10550 ]$ ps
        \item Sync $\rightarrow$ SPAD B Delays:
        \item[] $ [-15580 ] $ ps
        \item SPAD A $\rightarrow$ SPAD B Delay: 
        \item[] [$-4940 ] $ ps
        \item Refer to Figure \ref{fig:optical_path_calibration_1MHz_run1}
    \end{itemize}

    \item Input delays into Swabian \texttt{tt.setInputDelay(SPAD, delay)}. 
    \item Refer to Figure \ref{fig:optical_path_calibrated_1MHz_run1}
    \item Proceed with $g^{(2)}(0)$ measurement  (Experiment procedure in subsection \ref{subsec:CorrVal}). 
    \item Reference Results in Figure \ref{fig:g2(0)_measurements_80MHz_1MHz_run1}, left graph.
    %\item We observe $g^{(2)}(0)$ value is higly dependent on the lenght of coincidence window. For 400 ps $g^{(2)}(0)=0.0001$ while for 100,000 ps we get $g^{(2)}(0)=0.9442$
\end{itemize} 


\textbf{Aumenting the LASER Frequency} 

\begin{itemize}
    \item LASER frequency is configured at $80MHz$. Select internal oscillator\textit{Source: Int1}, and Rep Frequency at \textit{1}
    \item Turn Laser On and calibrate cps. 
    \begin{itemize}
        \item Use \texttt{tt.Countrate} to extract average counts-per-second (cps) over $5*10^11$ picoseconds. 
        \item Averaging the previus averages 31 times, we get:
        \begin{itemize}
            \item Mean SPAD A: 839,914.581 cps
            \item Mean SPAD B: 683,993.507 cps
            \item Mean LASER: 79,999,739.641cps
        \end{itemize}
    \end{itemize}
    \item Then, we proceed with the calibration for the optical path differences. We use \texttt{tt.Correlation} function, with $10$ps resolution to find the time differences between two channels and obtained the following results. 
    \begin{itemize}
        \item Sync $\rightarrow$ SPAD A Delays:
        \item[] $ [ 1970  ]$ ps
        \item Sync $\rightarrow$ SPAD B Delays:
        \item[] $ [-3050 ] $ ps
        \item SPAD A $\rightarrow$ SPAD B Delay: 
        \item[] $[-4930] $ ps
        \item See $optical\_path\_calibration.png$
    \end{itemize}
    \item Proceed with $g^{(2)}(0)$ measurement  (Experiment procedure in subsection \ref{subsec:CorrVal}). 
    \item Reference Results in Figure \ref{fig:g2(0)_measurements_80MHz_1MHz_run1}, right graph.
\end{itemize}

As a secondary result, we extract the system jitter from the Figures \ref{fig:optical_path_calibration_1MHz_run1} and \ref{fig:optical_path_calibrated_1MHz_run1}. 
The results are summarized in Figure \ref{fig:jitter_run1}.


\subsection{Correlation Function}
Second measurements designed to extract the second order cohrence function $g^{(2)}(\tau)$.

\begin{enumerate}
    \item Feed the Pulsed Laser into the optical setup in figure \ref{fig:exp_lay}. 
    \item Select a Laser frequency: $1/T_{dead} \approx 2\cdot10^8 = 200 MHz$.
    \item Build Histogram of $G^{(2)}(\tau)$
    \item Normalize histogram by the average count of the time bins far from $\tau  = 0$ to get $g^{(2)}(\tau)$. 
\end{enumerate}

We expect $g^{(2)}(0) = 1$ with a comb structure for the Pulsed Laser. 
In contrast, for thermal sources, $1 < g^{(2)}(0) \leq 2$ and decays over coherence times. 
The width of this decay is a measure of the coherence time \footnote{Set by the diffuser speed}.

\subsubsection{Pulsed Laser Measurements (05.06.2026)}
Results are summarized in Figure \ref{fig:g2(tau)_measurement_80MHz_bin50ps}.

\begin{figure}[H]
    \centering
    \includegraphics[width = 0.5\textwidth]{Figures/g2(tau)_measurement_80MHz_bin50ps.png}
    \caption{Autocorrelation function measurement. }
    \label{fig:g2(tau)_measurement_80MHz_bin50ps}
\end{figure}
